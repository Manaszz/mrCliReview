# Разделение ответственности: CLI vs FastAPI

## 🎯 Главный принцип

**CLI = Анализ кода**  
**FastAPI = Оркестрация + Git/GitLab операции**

---

## 📋 Что делает CLI?

### ✅ CLI отвечает за:

```python
┌─────────────────────────────────────────────────────────────────┐
│ CLI (Cline / Qwen Code)                                         │
│ Зона ответственности: CODE ANALYSIS ONLY                       │
└─────────────────────────────────────────────────────────────────┘

1. Анализ кода
   ├─ Чтение файлов
   ├─ Парсинг AST (Abstract Syntax Tree)
   ├─ Статический анализ
   └─ Вызов LLM для глубокого анализа

2. Обнаружение проблем
   ├─ Ошибки (bugs)
   ├─ Code smells
   ├─ Security vulnerabilities
   ├─ Performance issues
   └─ Best practices violations

3. Генерация рекомендаций
   ├─ Описание проблемы
   ├─ Предложение исправления
   ├─ Код snippet с примером
   └─ Объяснение почему это проблема

4. Опционально: Генерация документации
   ├─ Javadoc для методов
   ├─ Комментарии для сложного кода
   └─ Описание классов

5. Опционально: Автоматические исправления
   ├─ Форматирование кода
   ├─ Простые рефакторинги
   └─ Добавление import statements
```

### 📤 Что CLI отдаёт в ответ

**JSON структура:**

```json
{
  "review_type": "ERROR_DETECTION",
  "agent": "CLINE",
  "execution_time_seconds": 45.2,
  "timestamp": "2025-11-03T10:30:00Z",
  
  "issues": [
    {
      "file": "src/main/java/com/example/UserService.java",
      "line": 45,
      "severity": "HIGH",
      "category": "NullPointer",
      "message": "Potential NullPointerException: user.getEmail() can return null",
      "code_snippet": "String email = user.getEmail().toLowerCase();",
      "suggestion": "Add null check: if (user.getEmail() != null) { ... }",
      "auto_fixable": true,
      "cwe": "CWE-476",
      "rule_source": "custom_rules"
    },
    {
      "file": "src/main/java/com/example/UserService.java",
      "line": 67,
      "severity": "MEDIUM",
      "category": "SQLException",
      "message": "SQL exception not properly handled",
      "code_snippet": "try { stmt.execute(); } catch (Exception e) { }",
      "suggestion": "Log exception and handle specific SQLException cases",
      "auto_fixable": false
    }
  ],
  
  "refactoring_suggestions": [
    {
      "file": "src/main/java/com/example/UserService.java",
      "line": 120,
      "severity": "MEDIUM",
      "category": "CodeComplexity",
      "message": "Method createUser() is too long (150 lines)",
      "suggestion": "Extract validation logic to separate method",
      "impact": "SIGNIFICANT",
      "effort": "MEDIUM",
      "auto_fixable": false
    }
  ],
  
  "documentation_additions": [
    {
      "file": "src/main/java/com/example/UserService.java",
      "line": 25,
      "type": "JAVADOC",
      "generated_doc": "/**\n * Creates a new user account with validation.\n * \n * @param userData User registration data\n * @return Created user entity\n * @throws ValidationException if userData is invalid\n * @throws DuplicateEmailException if email already exists\n */",
      "reason": "Public method lacks documentation"
    }
  ],
  
  "files_modified": [
    {
      "file": "src/main/java/com/example/UserService.java",
      "changes": [
        {
          "type": "documentation",
          "line": 25,
          "content": "/** Javadoc comment */"
        },
        {
          "type": "auto_fix",
          "line": 45,
          "old": "String email = user.getEmail().toLowerCase();",
          "new": "String email = user.getEmail() != null ? user.getEmail().toLowerCase() : \"\";"
        }
      ]
    }
  ],
  
  "summary": {
    "total_issues": 15,
    "critical": 2,
    "high": 5,
    "medium": 6,
    "low": 2,
    "auto_fixable_count": 8,
    "files_analyzed": 12
  },
  
  "cli_metadata": {
    "version": "2.1.0",
    "model_used": "deepseek-v3.1-terminus",
    "tokens_used": 25000,
    "confidence_avg": 0.87
  }
}
```

---

## 📋 Что делает FastAPI?

### ✅ FastAPI отвечает за:

```python
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI Application                                             │
│ Зона ответственности: ORCHESTRATION + GIT/GITLAB               │
└─────────────────────────────────────────────────────────────────┘

1. GitLab API взаимодействие
   ├─ Получить MR metadata (source/target branch)
   ├─ Получить project info
   ├─ Создать MR (для refactoring)
   ├─ Добавить комментарии в MR
   └─ Post review notes

2. Git операции
   ├─ Clone repository с credentials
   ├─ Fetch target branch для diff
   ├─ Commit изменений (если CLI их сделал)
   ├─ Push в GitLab
   ├─ Create branches (для refactoring MR)
   └─ Cleanup (удаление локальных копий)

3. Оркестрация workflow
   ├─ Запустить CLI review
   ├─ Собрать результаты от CLI
   ├─ Классифицировать refactoring (significant/minor)
   ├─ Решить создавать ли отдельный MR
   ├─ Агрегировать результаты из нескольких review types
   └─ Форматировать для GitLab

4. Бизнес-логика
   ├─ Загрузка custom rules
   ├─ Интеграция с JIRA (TODO agent)
   ├─ Генерация CHANGELOG (TODO agent)
   ├─ Retry logic при ошибках
   └─ Логирование и мониторинг

5. Создание artifacts
   ├─ GitLab MR comments (markdown)
   ├─ Отдельные MR для refactoring
   ├─ Notes в MR
   └─ Notifications (опционально)
```

---

## 🔄 Полный Flow: Кто что делает

### Сценарий 1: Review с комментариями (только ремарки)

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. Запрос от n8n / пользователя                                │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: GET MR info через GitLab API                          │
│ ─────────────────────────────────────────────────────────       │
│ GET /api/v4/projects/123/merge_requests/456                    │
│ Response:                                                       │
│   source_branch: "feature-user-service"                         │
│   target_branch: "main"                                         │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Clone + Fetch через Git CLI                           │
│ ─────────────────────────────────────────────────────────       │
│ git clone --branch feature-user-service ...                    │
│ git fetch origin main:refs/remotes/origin/main                 │
│ Результат: /tmp/review/project-123-mr-456/                     │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Запустить CLI review                                  │
│ ─────────────────────────────────────────────────────────       │
│ cline review \                                                  │
│   --repo-path /tmp/review/project-123-mr-456 \                 │
│   --prompt /tmp/prompt-error-detection.md \                    │
│   --model deepseek-v3.1-terminus \                             │
│   --output /tmp/result-12345.json                              │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ CLI: Анализ кода (внутри)                                      │
│ ─────────────────────────────────────────────────────────       │
│ 1. git diff --name-only origin/main                            │
│    → [UserService.java, UserRepository.java]                   │
│                                                                 │
│ 2. Прочитать файлы                                             │
│    → Парсинг AST                                               │
│                                                                 │
│ 3. Статический анализ                                          │
│    → Найдено: NullPointer, SQL exception handling              │
│                                                                 │
│ 4. LLM анализ                                                  │
│    → DeepSeek: "Potential issues: ..."                         │
│                                                                 │
│ 5. Сформировать JSON результат                                 │
│    → Сохранить в /tmp/result-12345.json                        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼ CLI вернул JSON
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Обработать результаты CLI                             │
│ ─────────────────────────────────────────────────────────       │
│ result = json.load('/tmp/result-12345.json')                   │
│                                                                 │
│ issues = result['issues']  # 15 проблем                        │
│ refactoring = result['refactoring_suggestions']  # 3 штуки     │
│ documentation = result['documentation_additions']  # 5 штук    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Опубликовать комментарии в GitLab MR                  │
│ ─────────────────────────────────────────────────────────       │
│ Для каждой проблемы:                                           │
│   POST /api/v4/projects/123/merge_requests/456/notes           │
│   body: {                                                       │
│     "body": "⚠️ **HIGH**: Potential NullPointerException\n..."│
│   }                                                             │
│                                                                 │
│ Создать summary note:                                          │
│   POST /api/v4/projects/123/merge_requests/456/notes           │
│   body: {                                                       │
│     "body": "## 🤖 AI Review Summary\n\n..."                   │
│   }                                                             │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Cleanup                                                │
│ ─────────────────────────────────────────────────────────       │
│ rm -rf /tmp/review/project-123-mr-456                          │
│ rm /tmp/result-12345.json                                      │
│ rm /tmp/prompt-error-detection.md                              │
└─────────────────────────────────────────────────────────────────┘
```

**В этом сценарии:**
- ✅ CLI: Анализирует код, находит проблемы
- ✅ FastAPI: Клонирует, запускает CLI, публикует комментарии в GitLab
- ❌ CLI НЕ: коммитит, пушит, создаёт MR
- ❌ FastAPI НЕ: анализирует код

---

### Сценарий 2: Review с auto-documentation (коммит javadoc)

```
┌─────────────────────────────────────────────────────────────────┐
│ ... (шаги 1-5 те же) ...                                       │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼ CLI вернул JSON + изменил файлы
┌─────────────────────────────────────────────────────────────────┐
│ CLI: Генерация документации (если включено)                    │
│ ─────────────────────────────────────────────────────────       │
│ IF auto_documentation_enabled:                                  │
│   1. Сгенерировать Javadoc                                     │
│   2. Записать в файлы:                                         │
│      UserService.java: добавить /** ... */                     │
│      UserRepository.java: добавить /** ... */                  │
│   3. Вернуть в JSON:                                           │
│      "files_modified": [                                        │
│        {"file": "UserService.java", "changes": [...]}          │
│      ]                                                          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Обнаружить изменения в файлах                         │
│ ─────────────────────────────────────────────────────────       │
│ result = cli_result                                             │
│ if result.get('files_modified'):                               │
│     logger.info("CLI modified files, committing...")           │
│                                                                 │
│     # Проверить что действительно изменено                     │
│     changed = await git_manager.get_unstaged_files(repo_path)  │
│     # → ['UserService.java', 'UserRepository.java']            │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Commit изменений через Git CLI                        │
│ ─────────────────────────────────────────────────────────       │
│ await git_manager.commit_changes(                              │
│     repo_path=repo_path,                                        │
│     files=['UserService.java', 'UserRepository.java'],         │
│     commit_message="docs: Add Javadoc comments (AI generated)" │
│ )                                                               │
│                                                                 │
│ Внутри:                                                         │
│   git add UserService.java UserRepository.java                 │
│   git commit -m "docs: Add Javadoc comments (AI generated)"    │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Push в исходную ветку через Git CLI                   │
│ ─────────────────────────────────────────────────────────       │
│ await git_manager.push_branch(                                 │
│     repo_path=repo_path,                                        │
│     branch="feature-user-service",                             │
│     remote="origin"                                            │
│ )                                                               │
│                                                                 │
│ Внутри:                                                         │
│   git push origin feature-user-service                         │
│                                                                 │
│ Результат: Javadoc теперь в исходной MR ветке!                │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Добавить note в MR                                    │
│ ─────────────────────────────────────────────────────────       │
│ POST /api/v4/projects/123/merge_requests/456/notes             │
│ body: {                                                         │
│   "body": "✅ Added Javadoc documentation (commit abc123)"     │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

**В этом сценарии:**
- ✅ CLI: Генерирует Javadoc, ЗАПИСЫВАЕТ в файлы
- ✅ FastAPI: Обнаруживает изменения, КОММИТИТ, ПУШИТ
- ❌ CLI НЕ: коммитит, пушит (только пишет файлы)
- ❌ FastAPI НЕ: генерирует документацию

---

### Сценарий 3: Significant refactoring → отдельный MR

```
┌─────────────────────────────────────────────────────────────────┐
│ ... (шаги 1-5 те же) ...                                       │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼ CLI вернул refactoring suggestions
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Классифицировать refactoring                          │
│ ─────────────────────────────────────────────────────────       │
│ refactoring_classifier.classify(result['refactoring_suggestions'])│
│                                                                 │
│ Результат:                                                      │
│   significant_refactorings = [                                  │
│     {                                                           │
│       "type": "EXTRACT_METHOD",                                 │
│       "file": "UserService.java",                              │
│       "impact": "SIGNIFICANT"  ← Большое изменение             │
│     }                                                           │
│   ]                                                             │
│                                                                 │
│ IF significant_refactorings AND create_separate_mr:            │
│     → Создать отдельную ветку для refactoring                  │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Создать refactoring ветку через Git CLI               │
│ ─────────────────────────────────────────────────────────       │
│ # Checkout базовая ветка (обычно main)                         │
│ await git_manager.checkout_branch(                             │
│     repo_path=repo_path,                                        │
│     branch="origin/main"                                       │
│ )                                                               │
│                                                                 │
│ # Создать новую ветку                                          │
│ await git_manager.create_branch(                               │
│     repo_path=repo_path,                                        │
│     branch_name="refactor/user-service-extract-methods",       │
│     base_branch="origin/main"                                  │
│ )                                                               │
│                                                                 │
│ Внутри:                                                         │
│   git checkout origin/main                                     │
│   git checkout -b refactor/user-service-extract-methods        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ CLI: Применить refactoring (опционально)                       │
│ ─────────────────────────────────────────────────────────       │
│ IF auto_refactoring_enabled:                                    │
│   cline refactor \                                              │
│     --repo-path /tmp/review/... \                              │
│     --refactoring-plan /tmp/refactoring-plan.json \            │
│     --apply                                                     │
│                                                                 │
│   CLI применяет:                                                │
│     - Extract method                                            │
│     - Rename variables                                          │
│     - Simplify logic                                            │
│                                                                 │
│ ELSE:                                                           │
│   FastAPI создаёт скелет MR с описанием refactoring           │
│   Разработчик применяет вручную                                │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Commit refactoring через Git CLI                      │
│ ─────────────────────────────────────────────────────────       │
│ await git_manager.commit_changes(                              │
│     repo_path=repo_path,                                        │
│     files=['UserService.java'],                                │
│     commit_message="refactor: Extract validation methods\n\n" +│
│                    "- Extracted validateEmail()\n" +           │
│                    "- Extracted validatePhone()\n" +           │
│                    "Suggested by AI code review"               │
│ )                                                               │
│                                                                 │
│ Внутри:                                                         │
│   git add UserService.java                                     │
│   git commit -m "refactor: Extract validation methods..."      │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Push refactoring ветки через Git CLI                  │
│ ─────────────────────────────────────────────────────────       │
│ await git_manager.push_branch(                                 │
│     repo_path=repo_path,                                        │
│     branch="refactor/user-service-extract-methods",            │
│     remote="origin"                                            │
│ )                                                               │
│                                                                 │
│ Внутри:                                                         │
│   git push origin refactor/user-service-extract-methods        │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Создать MR через GitLab API                           │
│ ─────────────────────────────────────────────────────────       │
│ POST /api/v4/projects/123/merge_requests                       │
│ body: {                                                         │
│   "source_branch": "refactor/user-service-extract-methods",    │
│   "target_branch": "main",                                     │
│   "title": "🤖 [AI] Refactor: Extract validation methods",    │
│   "description": "## AI Suggested Refactoring\n\n" +           │
│                  "**Impact**: Significant\n" +                 │
│                  "**Effort**: Medium\n\n" +                    │
│                  "### Changes:\n" +                            │
│                  "- Extracted validateEmail()\n" +             │
│                  "- Extracted validatePhone()\n\n" +           │
│                  "### Benefits:\n" +                           │
│                  "- Improved readability\n" +                  │
│                  "- Better testability\n" +                    │
│                  "- Reduced method complexity"                │
│ }                                                               │
│                                                                 │
│ Response:                                                       │
│   { "iid": 789, "web_url": "https://gitlab.../789" }          │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│ FastAPI: Добавить ссылку в исходный MR                         │
│ ─────────────────────────────────────────────────────────       │
│ POST /api/v4/projects/123/merge_requests/456/notes             │
│ body: {                                                         │
│   "body": "✨ Created refactoring MR: !789\n\n" +             │
│            "Suggested significant refactorings have been " +    │
│            "moved to a separate MR for review."                │
│ }                                                               │
└─────────────────────────────────────────────────────────────────┘
```

**В этом сценарии:**
- ✅ CLI: Предлагает refactoring, может применить изменения
- ✅ FastAPI: Классифицирует, создаёт ветку, коммитит, пушит, создаёт MR
- ❌ CLI НЕ: создаёт ветки, коммитит, пушит, создаёт MR
- ❌ FastAPI НЕ: решает КАК рефакторить (только выполняет)

---

## 📊 Таблица ответственности

| Операция | CLI | FastAPI | Примечание |
|----------|-----|---------|------------|
| **Анализ кода** | ✅ | ❌ | CLI основная задача |
| **Обнаружение багов** | ✅ | ❌ | Статический анализ + LLM |
| **Генерация Javadoc** | ✅ | ❌ | CLI пишет в файлы |
| **Запись в файлы** | ✅ | ❌ | CLI может изменять код |
| **git clone** | ❌ | ✅ | FastAPI с credentials |
| **git fetch** | ❌ | ✅ | FastAPI для target branch |
| **git diff** | ✅ | ✅ | Оба могут использовать |
| **git commit** | ❌ | ✅ | FastAPI контролирует |
| **git push** | ❌ | ✅ | FastAPI с credentials |
| **git branch create** | ❌ | ✅ | FastAPI для refactoring MR |
| **GitLab API: get MR** | ❌ | ✅ | FastAPI для metadata |
| **GitLab API: create MR** | ❌ | ✅ | FastAPI для refactoring |
| **GitLab API: add comment** | ❌ | ✅ | FastAPI публикует review |
| **Классификация refactoring** | ❌ | ✅ | FastAPI бизнес-логика |
| **Загрузка custom rules** | ❌ | ✅ | FastAPI оркестрация |
| **Retry logic** | ❌ | ✅ | FastAPI обработка ошибок |
| **Cleanup** | ❌ | ✅ | FastAPI удаляет репо |

---

## 🔧 Конфигурация: Что можно включить/выключить

### В конфигурации FastAPI

```python
# app/models.py

class ReviewRequest(BaseModel):
    project_id: int
    merge_request_iid: int
    review_types: List[ReviewType]
    agent: CLIAgent = CLIAgent.CLINE
    language: str = "java"
    
    # Опции что делать с результатами
    enable_auto_documentation: bool = False  # CLI генерирует Javadoc
    commit_documentation: bool = False       # FastAPI коммитит
    enable_auto_fixes: bool = False          # CLI применяет простые фиксы
    commit_auto_fixes: bool = False          # FastAPI коммитит
    create_refactoring_mr: bool = True       # FastAPI создаёт MR
    auto_apply_minor_refactoring: bool = False  # CLI применяет minor
    
    # Куда публиковать результаты
    post_mr_comments: bool = True            # FastAPI публикует в GitLab
    send_notifications: bool = False         # FastAPI шлёт уведомления
```

### Примеры использования

#### Пример 1: Только review (ничего не изменяем)
```json
{
  "project_id": 123,
  "merge_request_iid": 456,
  "review_types": ["ERROR_DETECTION", "BEST_PRACTICES"],
  "enable_auto_documentation": false,
  "enable_auto_fixes": false,
  "create_refactoring_mr": false,
  "post_mr_comments": true
}
```
**Результат:** CLI анализирует, FastAPI публикует комментарии в MR

#### Пример 2: Review + Auto-documentation
```json
{
  "project_id": 123,
  "merge_request_iid": 456,
  "review_types": ["DOCUMENTATION"],
  "enable_auto_documentation": true,
  "commit_documentation": true,
  "post_mr_comments": true
}
```
**Результат:** CLI генерирует Javadoc → пишет в файлы → FastAPI коммитит + пушит → публикует note

#### Пример 3: Full automation
```json
{
  "project_id": 123,
  "merge_request_iid": 456,
  "review_types": ["ALL"],
  "enable_auto_documentation": true,
  "commit_documentation": true,
  "enable_auto_fixes": true,
  "commit_auto_fixes": true,
  "create_refactoring_mr": true,
  "auto_apply_minor_refactoring": true,
  "post_mr_comments": true
}
```
**Результат:** 
- CLI анализирует всё
- CLI генерирует Javadoc + применяет простые фиксы
- FastAPI коммитит изменения в исходную ветку
- FastAPI создаёт отдельный MR для significant refactoring
- FastAPI публикует комментарии

---

## 💡 Итоговый ответ

### Что CLI отдаёт в ответ?

**JSON с:**
1. **`issues`** - список найденных проблем
2. **`refactoring_suggestions`** - предложения по рефакторингу
3. **`documentation_additions`** - сгенерированная документация
4. **`files_modified`** - файлы которые CLI изменил (если разрешено)
5. **`summary`** - статистика

### Кто что делает?

#### CLI делает:
- ✅ Анализ кода
- ✅ Обнаружение проблем
- ✅ Генерация Javadoc (пишет в файлы)
- ✅ Применение auto-fixes (пишет в файлы)
- ❌ НЕ коммитит
- ❌ НЕ пушит
- ❌ НЕ создаёт MR
- ❌ НЕ создаёт ветки

#### FastAPI делает:
- ✅ Клонирует репозиторий
- ✅ Запускает CLI
- ✅ Обнаруживает изменения от CLI
- ✅ Коммитит изменения (если CLI изменил файлы)
- ✅ Пушит в GitLab
- ✅ Создаёт refactoring ветки
- ✅ Создаёт refactoring MR
- ✅ Публикует комментарии в MR
- ❌ НЕ анализирует код

---

**Вывод:** Чистое разделение - CLI анализирует и пишет файлы, FastAPI делает всё остальное (Git + GitLab).


