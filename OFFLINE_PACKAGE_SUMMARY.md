# 📦 AI Code Review - Offline Deployment Package

## ✅ Подготовка завершена!

Для вас подготовлена полная документация и скрипты для создания offline deployment пакета с Cline CLI.

---

## 🎯 Что подготовлено

### 📝 Документация

| Файл | Назначение |
|------|-----------|
| `HOW_TO_BUILD_OFFLINE_PACKAGE.md` | **Инструкция по сборке пакета** (для машины с интернетом) |
| `OFFLINE_DEPLOYMENT_PACKAGE_README.md` | **Инструкция для конечных пользователей** (войдет в пакет) |
| `docs/WINDOWS_OFFLINE_INSTALL.md` | Детальная инструкция установки на Windows PC |
| `docs/LINUX_VPS_OFFLINE_INSTALL.md` | Детальная инструкция установки на Linux VPS |
| `OFFLINE_QUICK_START.md` | Краткое руководство по offline развертыванию |

### 🔧 Скрипты и конфигурация

| Файл | Назначение |
|------|-----------|
| `scripts/prepare-full-offline.sh` | **Автоматическая сборка offline пакета** |
| `docker-compose.offline.yml` | Docker Compose для offline режима |
| `Dockerfile.offline` | Dockerfile для offline сборки |

---

## 🚀 Как создать offline пакет

### Вариант 1: Автоматическая сборка (РЕКОМЕНДУЕТСЯ)

**На машине с интернетом:**

```bash
# 1. Перейти в корень проекта
cd /workspace

# 2. Запустить скрипт автоматической подготовки
./scripts/prepare-full-offline.sh
```

**Что произойдет:**
1. ✅ Скачаются Cline CLI и Qwen Code CLI из npm
2. ✅ Скачаются все Python зависимости
3. ✅ Скачается базовый Docker образ (Python + Node.js)
4. ✅ Соберется Docker образ с установленными CLI
5. ✅ Создастся архив `code-review-offline-YYYYMMDD.tar.gz` (~2-3 GB)

**Результат:**
```
code-review-offline-20251106.tar.gz  (готов к передаче!)
```

### Вариант 2: Ручная сборка

См. детальную инструкцию в `HOW_TO_BUILD_OFFLINE_PACKAGE.md`

---

## 📤 Передача на изолированные системы

### Windows PC

1. **Скопируйте файл** на изолированный Windows ПК
2. **Распакуйте** архив (7-Zip / WinRAR)
3. **Откройте PowerShell** от администратора
4. **Запустите**: `.\install-windows.ps1`
5. **Настройте** `.env` файл (откроется автоматически)
6. **Запустите**: `docker-compose -f docker-compose.offline.yml up -d`

**Детально:** `docs/WINDOWS_OFFLINE_INSTALL.md`

### Linux VPS

1. **Скопируйте файл** на сервер: `scp code-review-offline-*.tar.gz user@server:/opt/`
2. **Распакуйте**: `tar -xzf code-review-offline-*.tar.gz`
3. **Запустите установку**: `bash install-linux.sh`
4. **Настройте** `.env`: `nano .env`
5. **Запустите**: `docker-compose -f docker-compose.offline.yml up -d`

**Детально:** `docs/LINUX_VPS_OFFLINE_INSTALL.md`

---

## ✅ Что входит в пакет

### Docker образы
- `base-python-nodejs.tar` - Базовый образ (Python 3.11 + Node.js 18)
- `code-review-api.tar` - Образ системы с Cline CLI и Qwen Code CLI

### Зависимости
- **npm пакеты**: @cline/cli, @qwen-code/qwen-code
- **Python пакеты**: FastAPI, httpx, python-gitlab и все зависимости

### Исходный код
- **app/** - FastAPI приложение
- **prompts/** - Промпты для AI агентов
- **rules/** - Правила code review для Java Spring Boot

### Скрипты установки
- **install-linux.sh** - Автоустановка для Linux
- **install-windows.ps1** - Автоустановка для Windows

### Документация
- Полное описание системы
- Инструкции по установке
- Troubleshooting
- Примеры конфигурации

---

## 🔧 Требования для целевых систем

### Windows PC
- Windows 10/11 Pro или Enterprise (64-bit)
- Docker Desktop для Windows 4.25+
- 4GB RAM (рекомендуется 8GB+)
- 20GB свободного места

### Linux VPS
- Ubuntu 20.04+, Debian 11+, CentOS 8+, или RHEL 8+
- Docker 20.10+ и Docker Compose 2.0+
- 4GB RAM (рекомендуется 8GB+)
- 20GB свободного места

### Оба варианта требуют:
- Доступ к внутреннему Model API (DeepSeek/Qwen)
- Доступ к внутреннему GitLab

---

## 📊 Размеры

| Компонент | Размер |
|-----------|--------|
| Архив (сжатый) | ~2-3 GB |
| Распакованный | ~5-6 GB |
| Базовый образ | ~450 MB |
| API образ | ~2.5 GB |
| npm пакеты | ~5 MB |
| Python пакеты | ~80 MB |

---

## ⚙️ Конфигурация

После установки необходимо настроить `.env` файл:

```env
# ОБЯЗАТЕЛЬНЫЕ параметры
MODEL_API_URL=http://internal-model-api:8000/v1
MODEL_API_KEY=your-api-key
GITLAB_URL=https://gitlab.company.local
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Имена моделей
DEEPSEEK_MODEL_NAME=deepseek-v3.1-terminus
QWEN3_MODEL_NAME=qwen3-coder-32b

# Выбор CLI агента
DEFAULT_CLI_AGENT=CLINE
```

---

## 🔍 Проверка работы

После запуска проверьте:

```bash
# Health check
curl http://localhost:8000/api/v1/health

# Ожидаемый ответ:
{
  "status": "healthy",
  "cline_available": true,
  "qwen_available": true,
  "model_api_connected": true,
  "gitlab_connected": true
}
```

---

## 📚 Структура проекта

```
/workspace/
│
├── 📝 Документация по offline deployment
│   ├── HOW_TO_BUILD_OFFLINE_PACKAGE.md       ⭐ Как собрать пакет
│   ├── OFFLINE_DEPLOYMENT_PACKAGE_README.md  ⭐ Readme для пакета
│   ├── OFFLINE_QUICK_START.md                  Быстрый старт
│   ├── docs/WINDOWS_OFFLINE_INSTALL.md         Установка на Windows
│   └── docs/LINUX_VPS_OFFLINE_INSTALL.md       Установка на Linux
│
├── 🔧 Скрипты
│   ├── scripts/prepare-full-offline.sh       ⭐ Автосборка пакета
│   └── scripts/prepare-offline-build.sh        Сборка только npm
│
├── 🐳 Docker конфигурация
│   ├── docker-compose.offline.yml            ⭐ Для offline режима
│   ├── Dockerfile.offline                    ⭐ Offline Dockerfile
│   └── docker-compose.yml                      Стандартный
│
├── 📦 Offline пакеты (создаются при сборке)
│   └── offline-packages/
│       ├── *.tgz                               npm пакеты
│       └── pip/                                Python пакеты
│
├── 💻 Приложение
│   ├── app/                                    FastAPI приложение
│   ├── prompts/                                AI промпты
│   └── rules/                                  Правила ревью
│
└── 📄 Конфигурация
    ├── env.example.annotated                   Пример конфигурации
    └── requirements.txt                        Python зависимости
```

---

## 🎯 Типичный workflow

### 1. Подготовка (на машине с интернетом)

```bash
# Собрать offline пакет
./scripts/prepare-full-offline.sh

# Результат: code-review-offline-YYYYMMDD.tar.gz
```

### 2. Передача

```bash
# Скопировать на USB или через scp
scp code-review-offline-20251106.tar.gz user@target:/opt/
```

### 3. Установка на изолированной системе

**Linux:**
```bash
tar -xzf code-review-offline-20251106.tar.gz
cd code-review-offline-20251106
bash install-linux.sh
nano .env
docker-compose -f docker-compose.offline.yml up -d
```

**Windows:**
```powershell
# Распаковать архив
cd code-review-offline-20251106
.\install-windows.ps1
# Настроить .env
docker-compose -f docker-compose.offline.yml up -d
```

---

## 🆘 Поддержка

### Частые проблемы

**CLI не установлен:**
```bash
docker exec -it code-review-api bash
npm install -g /tmp/npm-packages/cline-*.tgz
```

**Не подключается к Model API:**
- Проверьте `MODEL_API_URL` в `.env`
- Для localhost используйте `host.docker.internal` (Windows) или `172.17.0.1` (Linux)

**Порт занят:**
```yaml
# В docker-compose.offline.yml изменить:
ports:
  - "8001:8000"
```

### Логи

```bash
# Просмотр логов
docker-compose logs -f

# Проверка ресурсов
docker stats
```

---

## 📞 Полезные команды

```bash
# Статус системы
docker-compose ps
curl http://localhost:8000/api/v1/health

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Просмотр использования ресурсов
docker stats

# Обновление конфигурации
nano .env
docker-compose restart
```

---

## 📋 Чек-лист

### Подготовка пакета
- [ ] Клонирован репозиторий
- [ ] Запущен `prepare-full-offline.sh`
- [ ] Создан архив `code-review-offline-YYYYMMDD.tar.gz`
- [ ] Создан checksum файл
- [ ] Проверена целостность архива

### Установка на целевой системе
- [ ] Docker установлен и запущен
- [ ] Архив скопирован и распакован
- [ ] Запущен install скрипт
- [ ] Настроен .env файл
- [ ] Система запущена
- [ ] Health check возвращает "healthy"
- [ ] CLI доступны (cline --version)
- [ ] Подключение к Model API успешно
- [ ] Подключение к GitLab успешно

---

## 🎉 Готово!

Вся необходимая документация и скрипты подготовлены. Следуйте инструкциям в:

1. **`HOW_TO_BUILD_OFFLINE_PACKAGE.md`** - для сборки пакета
2. **`docs/WINDOWS_OFFLINE_INSTALL.md`** - для установки на Windows
3. **`docs/LINUX_VPS_OFFLINE_INSTALL.md`** - для установки на Linux

**Удачного развертывания! 🚀**

---

**Версия:** 2.0.0  
**Дата:** 2025-11-06  
**Совместимость:** Windows 10/11, Ubuntu 20.04+, Debian 11+, CentOS 8+, RHEL 8+
