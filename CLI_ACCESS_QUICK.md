# 🔑 Быстрый ответ: Как CLI получают доступ к GitLab?

## ⚡ Короткий ответ

**CLI НЕ обращаются к GitLab!** Они работают только с локальными файлами.

---

## 🔄 Как это работает

```
.env файл → FastAPI → GitLab API → Git Clone → Локальные файлы → CLI
```

### 1️⃣ Конфигурация (`.env`)
```env
GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
```

### 2️⃣ FastAPI формирует URL с токеном
```
https://oauth2:TOKEN@gitlab.example.com/group/project.git
          ↑     ↑
       username token (ваш GITLAB_TOKEN)
```

### 3️⃣ Git CLI клонирует с этим URL
```bash
git clone \
  --branch feature-branch \
  https://oauth2:TOKEN@gitlab.example.com/repo.git \
  /tmp/review/project-123-mr-456
```

### 4️⃣ CLI работают с локальной копией
```bash
cline review \
  --repo-path /tmp/review/project-123-mr-456  ← Локальный путь!
  --files src/Main.java \
  --model deepseek-v3.1-terminus
```

**CLI не знает о GitLab вообще!**

---

## 🛠️ Настройка (3 шага)

### Шаг 1: Создать токен в GitLab
1. GitLab → Settings → Access Tokens
2. Права: `read_repository`, `write_repository`, `api`
3. Скопировать токен

### Шаг 2: Добавить в `.env`
```env
GITLAB_URL=https://gitlab.example.com
GITLAB_TOKEN=glpat-ваш-токен-здесь
```

### Шаг 3: Запустить
```bash
docker-compose up -d
```

**Готово!** Больше ничего настраивать не нужно.

---

## ✅ Проверка

```bash
# Проверить подключение
curl http://localhost:8000/api/v1/health

# Ожидаемый ответ:
{
  "gitlab_connected": true  ← Должно быть true
}
```

---

## 🚨 Если не работает

### "Authentication failed"
```bash
# 1. Проверить токен
curl -H "Private-Token: ВАШ_ТОКЕН" \
  https://gitlab.example.com/api/v4/user

# Должен вернуть ваши данные

# 2. Обновить токен в .env
# 3. Перезапустить
docker-compose restart
```

### "Repository not found"
- Проверить права токена: должен быть `read_repository`
- Проверить project_id: правильный ли?

---

## 📖 Подробности

См. [docs/CLI_GITLAB_ACCESS.md](docs/CLI_GITLAB_ACCESS.md) для полной документации.

---

## 🎯 Ключевые моменты

1. ✅ **CLI не нужен доступ к GitLab** - работают только с локальными файлами
2. ✅ **Токен передаётся через Git clone URL** - формат `oauth2:TOKEN@domain`
3. ✅ **Единственная настройка** - `GITLAB_TOKEN` в `.env`
4. ✅ **Безопасность** - токен не логируется, репозитории удаляются после review
5. ✅ **Автоматически** - Docker подхватывает `.env` при старте


